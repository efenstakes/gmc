import styled from "styled-components";

const Row = styled.div`
  padding: 20px 0;
  display: flex;
  flex-flow: row wrap;
  justify-content: space-between;
  align-items: center;
`;

export default Row;
