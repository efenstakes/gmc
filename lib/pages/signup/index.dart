import 'package:flutter/material.dart';

class SignUpIndex extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('sign up works'),
      ),
    );
  }
}
